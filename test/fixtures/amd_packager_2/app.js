define(function(require) {
    var util = require('./util/index.js');
    var each = require('util/each');


    return function init() {
        each([], function() {
            // empty
        });
    }
});